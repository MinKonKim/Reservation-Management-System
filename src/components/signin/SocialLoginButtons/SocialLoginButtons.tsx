const SocialLoginButtons = () => {
  /*TODO: 소셜 로그인 컴포넌트화 필요. */
  return (
    <div className="border-t border-b  flex h-[16vh] items-center justify-between">
      <button>구글 로그인</button>
      <button>네이버 로그인</button>
    </div>
  );
};

export default SocialLoginButtons;
