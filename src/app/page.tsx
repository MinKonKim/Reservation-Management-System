"use client";

const Home = () => {
  return (
    <div className="flex h-screen items-center justify-center ">
      <button className="p-5 bg-blue-600 rounded-lg">DB연결확인버튼</button>
    </div>
  );
};

export default Home;
